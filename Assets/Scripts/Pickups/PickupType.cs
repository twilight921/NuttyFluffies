// The two collectible kinds from the reference game: Hearts (score, often
// floated in risky spots off the rail) and Coins (currency, usually sat along
// the easy line). Standalone file (not nested), matching PowerUpType, so it
// shows up cleanly in the Inspector and MCP tooling.
public enum PickupType
{
    Heart,
    Coin
}
