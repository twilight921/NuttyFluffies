// The set of special-carriage abilities a PowerUpCart can carry, matching the
// reference game's rocket / jump-jet / magnet carriage variants. Standalone
// file (not nested) so it shows up cleanly in the Inspector and MCP tooling.
public enum PowerUpType
{
    Rocket,
    JumpJet,
    Magnet
}
